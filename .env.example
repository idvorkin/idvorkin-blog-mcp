# GitHub Configuration
# GitHub username or organization (default: idvorkin)
GITHUB_REPO_OWNER=idvorkin

# Repository specification (default: *)
# - Single repo: "idvorkin.github.io"
# - Multiple repos: "repo1,repo2,repo3" (comma-separated)
# - All repos: "*" (wildcard)
GITHUB_REPOS=*

# Default repository when none specified (default: idvorkin.github.io)
DEFAULT_REPO=idvorkin.github.io

# Public blog URL (default: https://idvork.in)
BLOG_URL=https://idvork.in

# Path to back-links.json in each repo (default: back-links.json)
BACKLINKS_PATH=back-links.json

# GitHub Personal Access Token (optional, but recommended)
# Increases API rate limit from 60/hour to 5,000/hour
# Get token from: https://github.com/settings/tokens
# Scopes needed: public_repo (or repo for private repos)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
